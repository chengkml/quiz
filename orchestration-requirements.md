# 通用编排管理功能需求说明书

## 1. 背景与目标

- 在系统内存在众多“能力组件”（如：任务执行、数据处理、接口调用、条件判断、通知推送等），需要通过可视化方式灵活组成业务流程。
- 希望提供一个通用的编排管理能力，支持通过拖拽组件到画布、连线、配置参数的方式，定义并维护业务工作流，而不用频繁改代码。
- 该能力需要具备可复用、可扩展、可观测的特点，支持后续接入更多组件类型和执行引擎。

目标：

- 提供一个通用的编排组件定义模型，可被不同业务复用。
- 提供一个可视化画布，用于搭建、编辑、查看组件编排工作流。
- 提供一个执行与监控闭环：工作流可以被触发、执行，并能够查看执行状态与历史。
- 提供基础的版本、发布、权限控制能力，确保编排在不同环境下可控。

## 2. 术语与角色

### 2.1 术语

- 编排组件（Node / Component）  
  抽象后的功能单元，如“HTTP 请求”“脚本执行”“条件判断”“延时”“并行分支”“子流程”等。  
  组件具备：
  - 元数据：名称、类型、描述、所属分类
  - 输入：输入参数定义
  - 输出：输出数据结构定义
  - 配置：运行所需的配置项（URL、超时时间、重试策略等）

- 工作流（Workflow / Flow）  
  由多个编排组件通过连线（边）组成的有向图，用于描述某一完整业务过程。

- 画布（Canvas）  
  前端可视化区域，用于展示和编辑工作流拓扑结构：节点、连线、分支条件等。

- 连线（Edge）  
  用于连接两个组件节点，表示执行流向。连线可携带条件（如表达式）。

- 实例（Workflow Instance / Run）  
  某个工作流在某一次被触发时对应的执行实例，记录所有节点的执行结果、耗时、日志等。

- 模板（Template）  
  带默认组件和连线的预置工作流，用于快速创建新工作流。

### 2.2 角色

- 业务配置人员（编排设计者）
  - 在画布上创建、编辑、发布工作流。
  - 不需要具备开发能力，只需理解业务流程。

- 开发人员
  - 定义新的编排组件类型（比如新增一种“调用某系统接口”组件）。
  - 对接执行引擎与外部系统。

- 运维 / 管理员
  - 管控不同环境（测试 / 预发 / 生产）的工作流版本与发布。
  - 监控执行情况，排查失败实例。

## 3. 业务场景与用例

### 3.1 主要场景

1. 业务人员通过画布创建新的业务工作流
   - 从组件库拖拽组件到画布。
   - 通过连线定义执行顺序和条件。
   - 配置各组件的参数、输入输出映射。
   - 保存草稿，调试，最终发布。

2. 对现有工作流进行调整
   - 基于已有工作流创建新版本。
   - 增加或删除组件，调整连线与条件。
   - 回滚到历史版本。

3. 执行与监控
   - 手动触发工作流测试。
   - 由外部系统（如定时任务、接口调用、事件）触发工作流。
   - 在线查看某个工作流实例的执行拓扑、每个节点的状态和耗时。
   - 对失败节点进行重试、跳过、重新执行等操作（视需求开放）。

4. 组件扩展
   - 开发新增组件类型，并通过配置注册到组件库。
   - 在画布中即可使用新组件。

## 4. 功能需求

### 4.1 编排组件定义

- 组件类型管理
  - 支持按分类展示，如“基础控制类”“系统集成类”“数据处理类”“通知类”等。
  - 每种组件类型需定义：
    - 全局唯一标识（type key）
    - 展示名称、图标
    - 功能描述
    - 输入参数定义（名称、类型、是否必填、默认值）
    - 输出数据结构定义
    - 运行配置项（如超时、重试次数、并发限制等）
  - 支持组件元数据的配置化管理（如存储于数据库，可在后台维护）。

- 组件输入输出与数据映射
  - 支持在工作流层面配置组件的输入来源：
    - 上游节点的输出字段
    - 工作流全局上下文（启动参数）
    - 常量表达式
  - 支持输出写入到工作流上下文中的指定路径。

- 内置基础组件（示例）
  - 启动节点（Start）
  - 结束节点（End）
  - 条件判断（If / Switch）
  - 并行分支（Parallel / Fork-Join）
  - 延时或定时触发（Delay / Timer）
  - HTTP 请求组件（HTTP Call）
  - 脚本执行组件（Script）
  - 子工作流调用组件（Subflow）

### 4.2 画布编排能力

- 画布基本操作
  - 拖拽组件到画布创建节点。
  - 选中节点进行移动、复制、删除。
  - 支持缩放、平移、对齐辅助线。
  - 支持多选、框选操作。

- 连线与控制流
  - 在节点间通过拖动连线端点创建连线。
  - 支持删除连线、修改连线起止节点。
  - 连线可配置：
    - 条件表达式（如 `status == "SUCCESS"`）
    - 优先级（当多个条件满足时的执行顺序）。
  - 支持常见控制结构：
    - 顺序执行：节点 A → 节点 B
    - 条件分支：If / Switch
    - 并行执行：Fork / Join
    - 循环结构：基于条件表达式的循环（可选，需约束防止死循环）。

- 属性面板
  - 点击节点，右侧弹出属性面板：
    - 基本信息：名称、描述、标签等。
    - 输入参数配置：来源选择、表达式编辑。
    - 配置项：超时、重试策略等。
  - 点击连线，弹出连线配置：
    - 条件表达式编辑
    - 优先级设置
    - 说明备注。

- 画布校验
  - 保存或发布前进行拓扑合法性检查：
    - 必须存在唯一的 Start 和至少一个 End。
    - 不允许存在无入或无出孤立节点（除 Start/End 外）。
    - 不允许形成无意义的环路（对有意设计的循环需支持但给出告警）。
  - 对必填参数、表达式语法进行校验，给出错误提示。

### 4.3 工作流生命周期管理

- 创建、编辑与保存
  - 支持创建新工作流，填写名称、编码、所属业务线、描述等。
  - 编辑工作流时支持：
    - 保存为草稿（Draft）
    - 提交审核（可选，如有审批流程）
    - 发布为正式版本（Published）。

- 版本管理
  - 每次发布生成新的版本号（如 v1、v2 等）。
  - 支持查看历史版本详情（含拓扑图）。
  - 支持将某个历史版本“另存为”新工作流或新版本。
  - 支持版本比较（至少在后续阶段支持差异查看）。

- 状态管理
  - 工作流状态示例：
    - 草稿（Draft）
    - 待发布（Pending）
    - 已发布（Published）
    - 已下线（Disabled）
  - 运行时仅允许指定环境使用已发布版本。

### 4.4 执行与触发

- 触发方式
  - 手动触发：在管理页面选择工作流及版本，输入启动参数后触发。
  - 定时触发（可选）：与调度系统集成，定时执行某个工作流。
  - 外部系统触发：
    - 提供 API 接口：`startWorkflow(workflowCode, version?, inputPayload)`。
    - 或消息或事件触发（如消息队列、事件总线）。

- 执行引擎（高层需求）
  - 能根据工作流定义的拓扑顺序调度各节点执行。
  - 支持同步与异步节点：
    - HTTP 调用同步返回。
    - 异步任务可通过回调或轮询更新状态。
  - 支持失败重试策略：
    - 重试次数、间隔、退避策略。
  - 支持并发控制：
    - 整体工作流并发度限制。
    - 某些关键组件的串行或限流执行。

### 4.5 监控与可观测性

- 实例列表
  - 按工作流、时间区间、状态过滤查询执行实例。
  - 显示关键信息：实例 ID、工作流、版本、触发方式、启动时间、结束时间、整体状态、错误摘要等。

- 实例详情
  - 以拓扑图形式展示该实例的实际执行情况：
    - 每个节点的状态（未执行、进行中、成功、失败、跳过）。
    - 每个节点的开始或结束时间、耗时。
  - 支持查看节点的输入输出数据（考虑敏感字段脱敏）。
  - 支持查看节点的执行日志（如 HTTP 请求或响应摘要、脚本输出）。

- 告警与通知（可选）
  - 对失败实例、超时实例进行告警通知（邮件或消息）。
  - 支持配置阈值（如连续失败次数、失败比例等）。

## 5. 权限与多环境支持

- 权限控制
  - 仅特定角色可以创建和编辑工作流。
  - 发布、下线操作可配置为需要更高权限或审批。
  - 监控页面可对部分敏感数据做权限控制或脱敏。

- 多环境（Dev、Test、Pre、Prod）
  - 工作流在不同环境可有不同的发布状态。
  - 支持从上游环境同步工作流定义到下游环境（如从测试同步到预发或生产）。
  - 同一工作流在不同环境的组件配置可存在差异（如调用的服务地址不同）：
    - 建议通过配置中心或环境变量解决，不强依赖画布层面。

## 6. 非功能性需求

- 可扩展性
  - 组件类型定义采用配置加插件化设计，方便新增组件。
  - 工作流执行引擎可支持水平扩展。

- 性能
  - 画布操作延迟低，支持在复杂工作流（如百级节点）下顺畅拖拽与缩放。
  - 执行引擎在高并发触发下保持稳定。

- 可靠性
  - 工作流定义与实例数据需持久化，避免单点故障。
  - 对关键配置变更提供审计与操作日志。

- 安全性
  - 控制对敏感参数的访问（如密码、Token），不在日志中明文暴露。
  - API 接口需鉴权与限流。

## 7. 数据模型（初步概念设计）

仅做概念级说明，具体字段可在设计阶段细化。

- 组件类型（ComponentType）
  - id
  - key（唯一类型标识）
  - name
  - category
  - icon
  - inputSchema（JSON）
  - outputSchema（JSON）
  - configSchema（JSON）
  - enabled

- 工作流定义（WorkflowDefinition）
  - id
  - code（业务唯一编码）
  - name
  - description
  - bizDomain 或 owner
  - status（Draft、Published、Disabled）
  - currentVersionId

- 工作流版本（WorkflowVersion）
  - id
  - workflowId
  - versionNumber
  - definitionGraph（整个拓扑的 JSON）
  - createdBy、createdAt
  - publishedBy、publishedAt
  - remark

- 工作流实例（WorkflowInstance）
  - id
  - workflowId
  - workflowVersionId
  - status
  - triggerType（Manual、API、Schedule、Event）
  - triggerParams（JSON）
  - startTime、endTime
  - errorSummary

- 节点实例（NodeInstance）
  - id
  - workflowInstanceId
  - nodeKey（在拓扑中的节点标识）
  - componentTypeKey
  - status
  - inputData（JSON）
  - outputData（JSON）
  - logs（如引用日志存储）
  - startTime、endTime
  - errorMessage

## 8. 前端页面与交互

- 工作流列表页
  - 展示工作流名称、编码、版本、状态、最近修改时间等。
  - 支持搜索、过滤、分页。
  - 支持创建、编辑、复制、下线操作入口。

- 工作流编辑页（画布）
  - 左侧：组件库。
  - 中间：画布区域。
  - 右侧：属性面板（根据选中对象变化：工作流、节点、连线）。
  - 顶部：工具栏（保存、发布、校验、放大缩小、对齐等）。
  - 支持从模板创建新工作流。

- 执行监控页
  - 实例列表页。
  - 实例详情页（含拓扑和节点状态）。

## 9. 后续扩展方向

- 支持工作流之间的事件触发与编排（事件驱动架构）。
- 支持工作流在线调试（单步执行、断点、修改上下文后继续）。
- 引入规则引擎、决策表与编排深度集成。
- 提供“低代码表单 + 编排”的组合能力，面向更广泛业务人员。

