# 大模型 RAG 方案 —— 知识集管理功能需求文档

## 一、背景与目标

### 1.1 现状说明

- 已有：系统已实现基础问答功能（基于大模型的对话问答）。
- 问题：当前问答对企业/业务自有知识的利用不足，无法精确、可控地基于特定知识源进行回答。

### 1.2 建设目标

- 引入 RAG（Retrieval-Augmented Generation，检索增强生成）方案，通过“知识集管理”能力，将文档、FAQ、手册等业务知识有组织地管理、切分、向量化并供问答过程检索。
- 让业务人员/运营人员可以：
  - 自主创建和维护“知识集”；
  - 管理知识集中的文档、片段；
  - 配置与监控知识集在问答中的效果。

---

## 二、核心概念定义

- **知识集（Knowledge Set）**
  - 一组可被检索并用于问答增强的业务知识的集合。
  - 例如：“产品手册知识集”“售后客服知识集”“内部流程知识集”。

- **文档（Document）**
  - 知识集中的原始知识载体，如：PDF、Word、Excel、Markdown、纯文本、网页抓取内容等。

- **片段 / 块（Chunk）**
  - 将文档按一定策略切分后的最小检索单元，用于向量化和召回。

- **向量索引（Vector Index）**
  - 对片段进行向量化后存储在向量库中的索引结构，用于相似度检索。

- **绑定范围（Scope）**
  - 问答会话使用哪些知识集：
    - 全局知识集：对所有用户/所有会话生效；
    - 场景/业务线绑定：如“客服入口”默认绑定客服知识集；
    - 会话临时绑定：在创建会话时指定使用哪些知识集。

---

## 三、用户角色与权限

> 具体角色可根据现有系统角色命名；以下为推荐划分。

- **超级管理员**
  - 管理所有知识集（创建、编辑、删除、配置）。
  - 配置向量引擎、嵌入模型等系统级参数。
  - 查看所有统计与审计日志。

- **知识管理员 / 运营人员**
  - 在授权范围内创建和维护知识集及其文档。
  - 配置知识集的切分规则、检索参数。
  - 查看自己管理的知识集的使用统计。

- **普通业务用户**
  - 在问答界面选择或使用已配置好的知识集，不具备管理能力。

- **权限要求**
  - 知识集粒度的访问控制：谁可以查看、编辑、删除该知识集。
  - 文档操作权限与知识集保持一致（默认继承，可后续细化）。

---

## 四、功能范围概览

- 知识集生命周期管理
  - 创建、编辑、复制、启用/停用、删除。
- 文档管理
  - 文档上传、解析、预览、更新、删除。
  - 支持多种格式：txt、pdf、docx、xlsx、md 等（具体格式可分阶段支持）。
- 数据处理与向量化
  - 文本清洗、切分配置（chunk size、overlap 等）。
  - 嵌入模型选择、向量库写入、索引状态管理。
- 检索与问答配置
  - 检索参数配置：Top K、相似度阈值、过滤条件等。
  - 问答使用该知识集时的策略配置：是否必须基于知识回答、引用片段展示方式等。
- 监控与运维
  - 使用统计、质量反馈、审计日志等。

---

## 五、详细功能需求

### 5.1 知识集管理

#### 5.1.1 创建知识集

- 输入字段：
  - 名称（必填，在同租户/空间下唯一）；
  - 描述（可选）；
  - 标签（可选，如“客服”“产品”“内部”等）；
  - 可见范围 / 权限（公开、指定角色、指定用户组等）；
  - 默认语言（如中文/英文，用于选择合适嵌入模型和分词策略）。
- 创建后状态：
  - 初始状态为“未构建索引”或“初始化中”，提示需要上传文档并构建索引。

#### 5.1.2 编辑知识集

- 支持修改：
  - 名称、描述、标签、权限、默认检索参数等。
- 校验逻辑：
  - 名称唯一性检查；
  - 若存在“索引构建中”任务时，对部分字段的修改需限制或给出提示。

#### 5.1.3 启用 / 停用知识集

- 启用：
  - 知识集可被问答服务使用。
- 停用：
  - 保留数据但不参与检索；问答配置中不再可选或标记为不可用。

#### 5.1.4 删除知识集

- 前置条件：
  - 检查是否有在用场景（如某问答入口/机器人仍绑定该知识集）。
  - 强制二次确认（弹窗 + 输入知识集名称或关键字“DELETE”）。
- 删除内容：
  - 元数据记录；
  - 相关文档及其切分片段；
  - 向量索引数据（物理删除或逻辑删除，视实现而定）。

---

### 5.2 文档管理

#### 5.2.1 文档上传

- 支持多文件上传到指定知识集。
- 上传参数：
  - 文件名自动读取，可自定义标题；
  - 文件描述（可选）；
  - 文档标签（可选，如“FAQ”“手册”等）；
  - 文档语言（可显式设置，否则系统自动检测）。
- 上传成功后：
  - 触发“解析 + 切分 + 向量化构建”的异步任务。
  - 前端展示任务状态（排队中、处理中等）。

#### 5.2.2 文档解析

- 异步处理状态：
  - 排队中、解析中、解析失败、解析完成。
- 异常处理：
  - 对格式不支持或解析失败的文件，展示失败原因（如“无法提取文本”“文件损坏”等）。
- 解析结果可视化：
  - 页面可查看解析后的文本片段样例，便于确认效果。

#### 5.2.3 文档列表与详情

- 文档列表展示字段：
  - 文档标题、上传时间、上传人、处理状态、片段数量、最后更新时间。
- 筛选与搜索：
  - 支持按时间、状态、上传人、标签等过滤。
- 文档详情页：
  - 基本信息：标题、描述、标签、版本号等；
  - 文档原文预览（分页或分段展示）；
  - 切分后的片段列表预览（内容摘要、创建时间等）。

#### 5.2.4 文档更新与删除

- 更新文档：
  - 支持重新上传新版本文件（版本号递增）。
  - 触发重新解析与向量化任务。
  - 历史向量处理策略（建议默认替换旧版本向量，可作为系统参数配置）。
- 删除文档：
  - 删除文档及其所有片段向量。
  - 需要同步更新知识集的片段数量统计与索引状态。

---

### 5.3 切分与向量化配置

#### 5.3.1 全局默认配置

- 定义系统级默认：
  - chunk 长度（如 512 token）；
  - overlap（如 100 token）；
  - 默认嵌入模型；
  - 向量维度等。

#### 5.3.2 知识集级配置

- 配置项：
  - 切分策略：按行、按段落、按标点、按章节标题等；
  - chunk 长度（如 300–1000 字/Token 可调）；
  - chunk overlap（如 50–200 字/Token 可调）；
  - 嵌入模型选择（中文优化、多语言模型等）。
- 作用范围：
  - 对该知识集内的后续文档解析与向量化生效；
  - 参数修改后可手动触发“重建索引”。

#### 5.3.3 索引构建与状态管理

- 触发时机：
  - 新增文档；
  - 文档更新；
  - 切分/向量化配置变更。
- 任务状态：
  - 未开始、进行中、成功、失败。
- 历史记录：
  - 支持查看最近 N 次索引构建任务的时间、触发原因、状态、失败原因。

---

### 5.4 检索与问答配置

#### 5.4.1 知识集检索参数

- 参数示例：
  - Top K：一次检索返回的候选片段数；
  - 相似度阈值：低于阈值则视为“未命中”；
  - 过滤条件：按标签、文档类型、时间范围、上传人等。
- 配置范围：
  - 可全局默认；
  - 可知识集层面独立配置。

#### 5.4.2 问答使用策略

- 与现有问答入口的集成：
  - 为每个问答入口/机器人配置默认绑定知识集列表；
  - 可配置是否允许前端用户手动选择知识集。
- 回答策略（可作为配置项）：
  - **知识优先模式**：优先基于知识集检索结果生成回答，未命中时回退到通用大模型知识；
  - **仅知识模式**：必须基于知识集回答，未命中则提示“知识库无相关内容”；
  - 回答中是否附带引用片段（在回答下方展示“来源文档与片段”信息）。

---

### 5.5 监控与统计

#### 5.5.1 使用统计

- 统计维度：
  - 按知识集：调用次数、命中率、平均检索耗时；
  - 按文档：被命中次数 Top N 文档；
  - 按片段：被命中次数高的片段（用于发现“黄金知识点”）。
- 时间维度：
  - 支持按天、周、月查看趋势。

#### 5.5.2 质量反馈（后续可迭代）

- 用户评价：
  - 每次问答后支持“有用 / 无用”反馈；
  - 可选的文本反馈，说明哪里不准确。
- 管理员视图：
  - 低满意度问题列表；
  - 对应的命中知识集、文档与片段；
  - 用于指导知识内容优化与补充。

---

### 5.6 审计与安全

#### 5.6.1 操作日志

- 记录内容：
  - 创建 / 编辑 / 删除知识集；
  - 上传 / 删除文档；
  - 修改切分 / 向量化 / 检索参数；
  - 索引构建任务执行结果（时间、执行人、状态、失败原因）。

#### 5.6.2 访问控制

- 原则：
  - 所有知识集只能被有权限的用户管理与访问；
  - 与既有账号与权限体系集成（角色、组织架构、租户等）。

#### 5.6.3 数据安全

- 敏感数据标记：
  - 对敏感文档进行标记（如“仅内部可见”“机密”）；
  - 限制其在外部场景或对外产品中被使用。
- 存储与传输安全：
  - 向量数据和原文数据的存储安全策略（可在技术设计中进一步细化）。

---

## 六、与现有问答功能的集成

### 6.1 问答请求参数扩展

- 在现有问答接口中增加字段（示例）：
  - `knowledgeSetIds`（可选）：本次问答使用的知识集 ID 列表；
  - `useKnowledgeOnly`（可选）：是否仅基于知识集回答；
  - `topK` / `threshold`（可选）：本次请求临时覆盖默认检索参数。

### 6.2 响应结构扩展

- 增加字段：
  - `usedKnowledgeSetIds`：本次回答实际使用到的知识集；
  - `references`：命中的片段列表，包含：
    - 知识集名称 / ID；
    - 文档 ID、标题；
    - 片段内容摘要；
    - 片段在文档中的位置信息（页码、段落号等）。

### 6.3 会话上下文集成

- 会话级配置：
  - 在创建会话时指定绑定知识集；
  - 会话中多轮调用共享相同知识集配置（可在会话中动态变更）。
- 兼容已有逻辑：
  - 未绑定知识集时，保持现有纯大模型问答能力。

---

## 七、非功能性需求

### 7.1 性能

- 单次检索延迟：
  - 在正常负载下，检索 + 生成总耗时控制在业务可接受范围内（如 3–5 秒，可在性能测试阶段具体约定）。
- 索引构建：
  - 作为异步任务执行，不阻塞上传和基础管理操作。

### 7.2 可用性

- 降级策略：
  - 知识集管理或向量服务异常时，系统可降级为普通问答，不影响核心问答可用性（但不使用知识增强）。

### 7.3 可扩展性

- 向量库：
  - 向量库实现可插拔，预留接入多种向量数据库 / 检索引
  - 支持后续多集群、多租户扩展。

- 嵌入模型：
  - 预留多种嵌入模型接入方式（本地 / 远程 API），可按语言或场景选择。

### 7.4 易用性

- 交互体验：
  - 提供清晰的操作引导，如流程提示：
    - “创建知识集 → 上传文档 → 构建索引 → 绑定到问答入口”。
  - 对各类状态（解析中、构建索引中、失败等）提供清晰可见的提示与恢复操作。

---

## 八、版本规划建议（可选）

### 8.1 V1 —— 可用版本

- 功能范围：
  - 知识集创建 / 编辑 / 删除；
  - 文档上传与解析（支持 1–2 种主流格式，如 txt + pdf）；
  - 简单切分与向量化；
  - 问答侧支持指定知识集进行 RAG 检索增强。
- 目标：
  - 实现从“无 RAG”到“有 RAG”的闭环能力。

### 8.2 V2 —— 增强版本

- 功能范围：
  - 检索参数配置；
  - 使用统计；
  - 索引任务管理；
  - 基础审计日志与权限控制完善。

### 8.3 V3 —— 优化与高级能力

- 功能范围：
  - 质量反馈闭环（低满意度问题分析）；
  - 更多文档格式与来源支持；
  - 跨知识集高级检索策略（权重、优先级、多知识集合并等）。

