# HEARTBEAT.md - Quiz 会话心跳规则

默认不主动触发周期任务；仅在长会话或用户明确要求时执行。

## 触发时机

- 单次会话持续时间较长，且跨多个模块改动。
- 需求已实现但尚未形成可追溯记录。
- 用户要求做阶段性巡检。

## 心跳检查清单（轻量）

1. 模块索引检查
   - 新增或首次命中的业务模块是否已写入 `memory/ENGINEERING_MODULE_INDEX.md`。
2. 链路一致性检查
   - 前端页面/API 与后端 Controller 路径、字段是否一致。
3. 验证状态检查
   - 当前改动是否至少有一条可执行验证命令和结果。
4. 记忆沉淀检查
   - 是否在 `memory/YYYY-MM-DD.md` 记录改动结论、风险、验证。

## 输出要求

- 仅输出异常项和待办项。
- 无异常时输出：`heartbeat: clean`。
