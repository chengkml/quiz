# 2026-03-26

## 开发需求：新增知识点统计功能（REQ-271759657607896399）
- 结论：
  - 已完成前端页面实现、API 类型与请求补齐、路由注册。
  - 已通过需求开发脚本执行完整闭环，状态已回写为 `COMPLETED(100%)`。
- 改动文件：
  - `frontend/src/pages/StatisticsCenter/themes/KnowledgeMastery/index.tsx`
  - `frontend/src/pages/StatisticsCenter/api/index.ts`
  - `frontend/src/router/index.tsx`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `cd backend && gradle classes` ✅
  - `cd frontend && npm run build` ✅（存在仓库既有 webpack 体积 warning，不影响本次交付）
  - `python3 skills/quiz-requirement-analyze/scripts/analyze_requirement.py --action query --status OPEN,IN_PROGRESS --project-name quiz --max-items 20` ✅（返回 0 条，说明开发列表已清空）
- 风险：
  - 中：菜单权限若未配置 `knowledge-mastery`，页面不会在菜单中展示（路由已注册，不影响已实现代码）。

## 统计中心补齐：知识点统计路由入口与菜单路径对齐
- 结论：
  - 已补齐统计中心 URL 形态路由（`statistics-center/**`）到三类统计主题页面的映射。
  - 已新增菜单迁移脚本，确保“统计中心”下可出现“单词熟练度统计/知识点统计”入口并可跳转到对应前端页面。
- 改动文件：
  - `frontend/src/router/index.tsx`
  - `backend/src/main/resources/db/migration/V202603261950__add_statistics_center_theme_menus.sql`
- 验证：
  - `cd backend && gradle classes` ✅
  - `cd frontend && npm run build` ✅（webpack 既有体积 warning 仍存在，不影响通过）
- 风险：
  - 中：新增菜单依赖数据库 migration 执行后才会在侧边栏可见；若环境未升级到该 migration，仅可通过直链访问页面。

## 文件管理优化：路径规范化 + 同级重名冲突校验 + 目录重命名存储一致性修复
- 结论：
  - 已完成文件管理核心服务的稳定性优化，重点修复“目录重命名后底层存储未迁移”与“移动/重命名/建目录缺少重名校验”问题。
  - 变更保持原有接口与业务行为，未引入跨模块重构。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/file/service/impl/FileServiceImpl.java`
  - `backend/src/main/java/com/ck/quiz/file/repository/FileMetadataRepository.java`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `cd backend && gradle classes` ✅
  - `cd frontend && npm run build` ✅（存在仓库既有 webpack 体积 warning，不影响本次交付）
- 风险：
  - 中：目录重命名现在会同步调用存储层 move（仅文件对象），若外部存储权限不足会直接失败并回滚事务；属于预期的失败快路径。
  - 低：同级重名校验变严，历史上依赖“重复名称可共存”的异常数据在后续操作时会更早暴露冲突。

## 数据查询工具页优化（DataQuery）
- 结论：
  - 已完成数据查询工具页可用性与结构优化，保留原有“选数据源-写 SQL-执行查询”业务链路。
  - 新增 schema 选择、表名检索与缓存刷新能力；修复错误回显与结果展示细节；去除无实际价值的 Mock 按钮。
- 改动文件：
  - `frontend/src/pages/DataQuery/index.tsx`
  - `frontend/src/pages/DataQuery/style/index.less`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `cd frontend && npm run build` ✅（通过；存在仓库既有 webpack 体积 warning，不影响本次交付）
  - `cd backend && gradle classes` ✅（UP-TO-DATE）
- 风险：
  - 低：当前通过 `collectSchema` 获取表结构用于左侧树展示，首开/刷新时会触发一次较重元数据采集，已通过前端缓存减少重复请求。

