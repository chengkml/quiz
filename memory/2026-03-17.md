## 需求列表列优化（移除创建时间，新增优先级展示）
- 结论：已完成需求列表列调整。前端移除“创建时间”列，新增“优先级”列并补齐文案+颜色展示；后端已有 `priority` 字段，无需接口改造。
- 改动文件：
  - `frontend/src/pages/Requirement/index.tsx`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `cd frontend && npm run build` ✅（构建通过，存在既有 webpack 体积 warning）
  - `cd backend && gradle classes` ✅（通过）
- 风险：
  - 低风险。仅列表展示列调整，不影响接口入参/状态流转。
  - 若外部脚本依赖前端表格“创建时间”列文案或列序，可能需要同步（当前仓库内未发现导出/列绑定逻辑）。
