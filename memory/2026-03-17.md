## 需求列表列优化（移除创建时间，新增优先级展示）
- 结论：已完成需求列表列调整。前端移除“创建时间”列，新增“优先级”列并补齐文案+颜色展示；后端已有 `priority` 字段，无需接口改造。
- 改动文件：
  - `frontend/src/pages/Requirement/index.tsx`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `cd frontend && npm run build` ✅（构建通过，存在既有 webpack 体积 warning）
  - `cd backend && gradle classes` ✅（通过）
- 风险：
  - 低风险。仅列表展示列调整，不影响接口入参/状态流转。
  - 若外部脚本依赖前端表格“创建时间”列文案或列序，可能需要同步（当前仓库内未发现导出/列绑定逻辑）。

## 需求开发：258860410529123491（知识集来源支持 Markdown 直贴并转向量）
- 结论：已完成第一批高价值交付（后端类型与处理链路 + 前端配置与展示），可创建 MARKDOWN 来源并走现有向量任务链路。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/knowledgeset/entity/KnowledgeSource.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/service/impl/KnowledgeSourceServiceImpl.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/job/KnowledgeProcessingJob.java`
  - `frontend/src/pages/KnowledgeSource/components/AddEditKnowledgeSourceModal.tsx`
  - `frontend/src/pages/KnowledgeSource/index.tsx`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `cd backend && gradle classes` ✅
  - `cd frontend && npm run build` ✅（存在既有 Arco less deprecation 与 webpack 体积 warning）
  - 需求状态流转脚本：`start(30%) -> progress(60%,90%)` ✅
- 风险：
  - 中风险：`knowledge_source.content` 改为 `TEXT` 依赖运行环境 JPA 自动 DDL 或手工迁移；若生产未启用自动迁移，需要补充 ALTER 语句。
  - 低风险：本次未改检索接口与上传链路，仅扩展 MARKDOWN 分支，不影响 FILE/DB 既有流程。
