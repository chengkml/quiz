## 需求管理页面 React #130 修复
- 现象：线上 Requirement 页面打开即报 `Minified React error #130`，错误方向为 `Element type is invalid / got undefined`。
- 定位结论：根因在前端 `frontend/src/pages/Requirement/api/index.ts` 与 `frontend/src/pages/Requirement/index.tsx` 的近期改动把模块本身写坏，不是后端问题。
  - `api/index.ts` 中出现未定义的 `designRequirement` 被写入 default export，同时文件尾部还残留一段非法文本 `tList, ...`，模块导出结构已损坏。
  - `index.tsx` 文件尾部存在重复/残缺 JSX 与重复 `export default Requirement;`，并夹杂重复 `case "design"` 等异常拼接内容，导致 Requirement 页面模块解析/导出不稳定。
- 处理策略：按“最小修复”将 Requirement 前端两个文件回退到当前稳定基线，先恢复页面可正常加载；不扩散到后端。
- 验证：`cd frontend && npm run build` 通过（webpack compiled with warnings）。
- 上线状态：待上线。

## Requirement 后端编译失败修复
- 现象：CI/本地后端编译报 `Status.PENDING_DESIGN` not found。
- 根因：Requirement 后端状态设计改动只改了一半；实体 `Requirement.Status` 并未真正引入 `PENDING_DESIGN`，但 `Requirement.java` 的进度归零逻辑和 `RequirementServiceImpl.java` 的 design 状态校验残留了对 `PENDING_DESIGN` 的引用，导致状态定义与服务流转代码不一致。
- 处理：按最小修复删除残留的 `PENDING_DESIGN` 引用，统一回当前实际存在的状态集合（`PENDING_ANALYSIS` 等），恢复编译链路。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/project/entity/Requirement.java`
  - `backend/src/main/java/com/ck/quiz/project/service/impl/RequirementServiceImpl.java`
- 验证：`cd backend && gradle classes` 通过。
- 状态：待提交。
- 风险：当前仅修复编译链路；“待设计”状态需求本身尚未完整落地到后端状态枚举与前后端联动，后续若继续推进该需求，需要按完整链路重新实现。
