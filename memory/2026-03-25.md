# 2026-03-25

## 需求批量开发（Requirement 列表顺序执行）

### 结论
- 已按需求列表顺序完成 8/8 条需求开发，并逐条回写状态到 `COMPLETED(100%)`。
- 本次改动覆盖前端需求页/角色页/知识集页/系统消息页，以及后端标签查询、向量同步检查两个服务点。

### 改动点
1. 修正需求管理页面表格高度设置
- `frontend/src/pages/Requirement/index.tsx`
- 调整 `calculateTableScrollHeight` 为基于视口可用区计算，避免分页栏被挤出。

2. 优化需求管理页面需求状态标签颜色
- `frontend/src/pages/Requirement/index.tsx`
- `PENDING_ANALYSIS` 颜色改为 `magenta`，图标改为 `IconSearch`，与 `OPEN` 拉开区分。

3. 需求管理页面评审时 md 编辑器默认 preview
- `frontend/src/pages/Requirement/index.tsx`
- 仅评审弹窗 `MDEditor` 从 `preview="edit"` 改为 `preview="preview"`。

4. 修复标签查询接口报错
- `backend/src/main/java/com/ck/quiz/tag/service/impl/TagServiceImpl.java`
- RowMapper 主键映射从 `tag_id` 改为 `id`。

5. 修复角色管理页面布局问题
- `frontend/src/pages/Role/index.tsx`
- `frontend/src/pages/Role/style/index.less`
- 新增动态表格高度计算，替换固定 `tableScrollHeight={500}`；补充容器 `min-height/flex/overflow` 约束。

6. 知识集页面（问题定位为“来源按钮误显示同步检查抽屉”）
- `frontend/src/pages/KnowledgeSet/index.tsx`
- 点击“来源”时主动关闭其它抽屉并清理对应 id；来源抽屉 visible 增加 `&& !!drawerKnowledgeSetId` 保护。

7. 修复系统消息页面布局问题
- `frontend/src/pages/SystemMessage/index.tsx`
- 调整表格高度计算为视口可用区方案，减少 DOM 反向依赖导致分页被遮挡。

8. 修复向量检查报错
- `backend/src/main/java/com/ck/quiz/knowledgeset/service/impl/VectorServiceImpl.java`
- `buildSourceFilterCondition` 返回片段补齐空格边界，避免拼接 SQL 时出现语法异常。

### 验证
- 后端：`cd backend && gradle classes` ✅（本次会话多次执行，最终通过）
- 前端：`cd frontend && npm run build` ✅（本次会话多次执行，最终通过；保留仓库既有 webpack 体积 warning）

### 风险
- 低：前端布局/展示类改动主要影响各页面滚动与可视高度，业务接口未改。
- 低：后端两处修复均为局部字段映射与 SQL 片段拼接，不改接口协议与数据结构。
- 中：知识集第 6 条需求实际线上问题描述与“卡片化展示”不一致，本次按需求详情中给出的实际故障（来源按钮抽屉错位）完成修复并回写。
