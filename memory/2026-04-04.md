## 家庭作业页面优化
- 结论：已完成家庭作业页面新增/编辑交互从 Drawer 改为居中 Modal 的前端改造，并已将需求 285118603626287566 回写为 COMPLETED。
- 改动文件：
  - `frontend/src/pages/Homework/index.tsx`
  - `frontend/src/pages/Homework/style/index.less`
- 验证：
  - 未执行编译/构建检查。
  - 已人工核对 `git diff -- frontend/src/pages/Homework/index.tsx frontend/src/pages/Homework/style/index.less`，确认改动范围仅在家庭作业前端页面与样式。
  - 已按需求链路回写状态：`IN_PROGRESS(0/60/90)` -> `COMPLETED(100)`。
- 风险：
  - 低：仅前端弹窗承载容器与样式调整，未改后端接口、字段契约和数据结构。
  - 未验证项：未在浏览器实际点击确认 Modal 在低分辨率下的滚动体验与 Arco Modal footer 展示效果。

## 商品价格监控功能开发
- 结论：已完成商品价格监控 MVP 开发，已覆盖监控商品 CRUD、手动采集价格快照、价格趋势图、邮件阈值预警、菜单与路由接入；按要求未执行编译/构建与额外回归测试。
- 改动文件：
  - 后端：`backend/src/main/java/com/ck/quiz/price/**`
  - 前端：`frontend/src/pages/PriceMonitor/index.tsx`、`frontend/src/pages/PriceMonitor/api.ts`、`frontend/src/pages/PriceMonitor/style/index.less`
  - 路由：`frontend/src/router/index.tsx`
  - 数据库迁移：`backend/src/main/resources/db/migration/V202604042120__create_price_monitor_module.sql`
  - 文档：`memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - 未执行编译/构建检查。
  - 已人工静态核对新增 controller / service / repository / dto / entity 链路，以及前端 API、路由、页面入口是否闭合。
  - 已按需求链路回写状态：`IN_PROGRESS(0/60)`，待本次结果确认后写 `COMPLETED(100)`。
- 风险：
  - 中：首版采集为手动录入/半自动补录，不包含真实电商平台自动抓取与定时调度。
  - 中：前端页面未经过浏览器实点与编译校验，可能仍有样式或类型细节需收口。
  - 低：邮件预警复用现有通知链路，依赖用户邮箱与 SMTP 参数已配置。

## 热搜功能支持设置关注主题
- 结论：已完成热搜关注主题 MVP，补齐用户维度主题配置、热搜命中标签、只看关注筛选，以及前后端查询链路闭环；按要求未执行编译/构建检查。
- 改动文件：
  - 后端：`backend/src/main/java/com/ck/quiz/hotsearch/controller/HotSearchController.java`、`backend/src/main/java/com/ck/quiz/hotsearch/service/HotSearchService.java`、`backend/src/main/java/com/ck/quiz/hotsearch/service/impl/HotSearchServiceImpl.java`、`backend/src/main/java/com/ck/quiz/hotsearch/service/impl/HotSearchFollowTopicServiceImpl.java`
  - DTO：`backend/src/main/java/com/ck/quiz/hotsearch/dto/HotSearchQueryDto.java`、`backend/src/main/java/com/ck/quiz/hotsearch/dto/HotSearchRecordDto.java`
  - 前端：`frontend/src/pages/HotSearch/index.tsx`、`frontend/src/pages/HotSearch/api.ts`、`frontend/src/pages/HotSearch/style/index.less`
  - 文档：`memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - 未执行编译/构建检查。
  - 已静态核对前后端接口契约：`/api/hot-search/search` 新增 `followedOnly`，返回 `matchedTopics`；`/api/hot-search/follow-topic/*` 支持主题 CRUD。
  - 已确认关注主题表迁移文件与 Controller/Service/Repository 链路存在，当前补齐了基于登录用户的命中识别和页面管理入口。
- 风险：
  - 中：当前主题命中仅基于热搜标题做大小写不敏感包含匹配，未扩展到 `detailMarkdown/extraJson`。
  - 中：关注主题新增逻辑依赖 JPA 审计自动回填 `create_user`，本次未做运行态验证。
  - 低：前端新增 Drawer/Modal 交互，未做浏览器实点，样式细节仍可能需要微调。

## 商品信息聚合查看功能（需求分析）
- 结论：已完成需求 `285118603626288104` 的代码定位与分析，并已通过 `quiz-requirement-analyze` 链路回写，状态从 `PENDING_ANALYSIS` 流转为 `PENDING_REVIEW`。
- 参考文件：
  - `backend/src/main/java/com/ck/quiz/base/controller/BaseController.java`
  - `backend/src/main/java/com/ck/quiz/base/service/impl/BaseServiceImpl.java`
  - `backend/src/main/java/com/ck/quiz/base/entity/Model.java`
  - `backend/src/main/java/com/ck/quiz/hotsearch/controller/HotSearchController.java`
  - `backend/src/main/java/com/ck/quiz/hotsearch/service/impl/HotSearchServiceImpl.java`
  - `frontend/src/pages/Homework/index.tsx`
  - `frontend/src/pages/Todo/index.tsx`
  - `frontend/src/pages/HotSearch/index.tsx`
  - `frontend/src/router/index.tsx`
- 分析结论：
  - 当前仓库无现成商品聚合模块，适合按现有 CRUD 模式新增 `product` 领域模块。
  - 推荐采用“关注商品表 + 商品快照表 + 批量导入接口 + 前端基础对比视图”的最小可交付方案。
  - 商品抓取能力明确不纳入 quiz，本期仅承接 OpenClaw 导入结果的存取与展示。
- 验证：
  - 未执行编译/构建检查。
  - 已执行需求详情读取与分析回写脚本，确认写回成功：`writebackSuccess=true`，`statusAfterWriteback=PENDING_REVIEW`。
- 风险：
  - 中：需求中“同款商品识别”“导入幂等”“快照保留策略”“对比上限”仍需评审确认。
  - 未验证项：未进入开发阶段，尚未生成数据库 migration、前端页面与接口实现。

## 文件管理页面中文化
- 结论：已完成文件管理页面用户可见英文文案的中文化，改动仅限前端显示层，未调整接口、状态和交互行为。
- 改动文件：
  - `frontend/src/pages/FileManager/index.tsx`
- 验证：
  - 已执行 `cd frontend && npm run build`，构建通过。
  - 构建输出仅包含既有 webpack 体积告警和 Less 依赖告警，本次改动未引入新的 TypeScript/打包错误。
- 风险：
  - 低：本次仅替换页面文案与弹窗/确认框按钮文本，不涉及后端接口和数据结构。
  - 未验证项：未做浏览器实点，图标按钮在 hover/title 场景下的中文提示仅做静态校对。

## 各种文件上传转 Markdown 功能（方案分析）
- 结论：已完成现有链路梳理，推荐基于现有 `DocumentConversionController + DocumentConverter SPI + MdConvert 页面 + FileManager` 做最小闭环，不建议直接复用 `KnowledgeSource(FILE)` 作为上传转 Markdown 主链路。
- 参考文件：
  - `backend/src/main/java/com/ck/quiz/controller/DocumentConversionController.java`
  - `backend/src/main/java/com/ck/quiz/service/converter/DocumentConverter.java`
  - `backend/src/main/java/com/ck/quiz/service/converter/impl/WordConverter.java`
  - `backend/src/main/java/com/ck/quiz/service/converter/impl/PdfConverter.java`
  - `backend/src/main/java/com/ck/quiz/service/converter/impl/ExcelConverter.java`
  - `backend/src/main/java/com/ck/quiz/service/converter/impl/HtmlConverter.java`
  - `frontend/src/pages/MdConvert/index.tsx`
  - `frontend/src/pages/FileManager/index.tsx`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/job/KnowledgeProcessingJob.java`
- 分析结论：
  - 后端已存在 `/api/convert/document` 上传转 Markdown 半成品，但前端暂无入口，也缺少 `txt/md` 透传和更清晰的结果编排。
  - 建议 MVP 先支持 `doc/docx/pdf/xls/xlsx/html/txt/md` 单文件转 Markdown，并落在现有 `MdConvert` 页面新增“文件转 Markdown”标签页。
  - 若后续要接知识库，建议将转换结果按 `MARKDOWN` 类型写入知识来源，而不是把上传文件路径直接塞给 `KnowledgeSource(FILE)`。
- 验证：
  - 未执行编译/构建检查。
  - 已静态核对现有上传、文件存储、Markdown 转换、知识来源处理链路。
- 风险：
  - 中：扫描版 PDF、图片类文件当前无 OCR 链路，不应纳入首版承诺。
  - 中：`KnowledgeProcessingJob` 当前依赖本地文件路径，和 `FileManager` 抽象存储并不完全同构，直接复用会埋兼容性风险。
  - 低：首版若仅提供“上传即转 Markdown + 下载/复制”，无需新增数据库表，交付最快但不保留转换历史。

## 编排功能 Dify 风格改造
- 结论：已完成编排模块前端工作台改造，列表页改为专用工作流卡片视图，画布页改为三栏式工作流编辑器，整体视觉与交互向 Dify 工作流工作台靠拢；未改后端接口契约。
- 改动文件：
  - `frontend/src/pages/Orchestration/index.tsx`
  - `frontend/src/pages/Orchestration/index.less`
  - `frontend/src/pages/Orchestration/CanvasEditor.tsx`
  - `frontend/src/pages/Orchestration/statusMeta.ts`
  - `frontend/src/pages/Orchestration/FlowEditor/index.tsx`
  - `frontend/src/pages/Orchestration/FlowEditor/index.less`
  - `frontend/src/pages/Orchestration/FlowEditor/Sidebar.tsx`
  - `frontend/src/pages/Orchestration/FlowEditor/PropertiesPanel.tsx`
  - `frontend/src/pages/Orchestration/FlowEditor/nodeMeta.tsx`
  - `frontend/src/pages/Orchestration/FlowEditor/nodes/BaseNode.tsx`
  - `frontend/src/pages/Orchestration/FlowEditor/nodes/index.tsx`
  - `frontend/src/types/orchestration.ts`
- 验证：
  - 已执行 `cd frontend && npm run build`，构建通过。
  - 已顺手修正 `CanvasEditor` 版本接口响应读取错误，改为实际的 `res.data` 链路。
- 风险：
  - 中：本次主要是前端重构，未新增“按版本只读预览/草稿调试/节点执行日志”等 Dify 深层能力，当前仍是视觉与基础交互对齐。
  - 低：节点配置字段仍基于前端图数据自由存储，后端暂未对 `definitionGraph` 做更严格 schema 校验。
  - 未验证项：未做浏览器实点，React Flow 拖拽、缩放、移动端窄屏体验仅经过构建校验，仍建议实际操作一轮。

## 各种文件上传转 Markdown 功能（实现）
- 结论：已在现有 `MdConvert` 页面落地“文件转 Markdown”页签，支持上传并转换 `doc/docx/pdf/xls/xlsx/html/txt/md`，提供预览、复制、下载 `.md`、载入 Markdown 编辑区；后端补齐文本直转与失败降级提取链路。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/controller/DocumentConversionController.java`
  - `backend/src/main/java/com/ck/quiz/controller/dto/DocumentConvertResponse.java`
  - `backend/src/main/java/com/ck/quiz/service/converter/impl/TextConverter.java`
  - `backend/src/main/java/com/ck/quiz/service/converter/impl/WordConverter.java`
  - `frontend/src/services/mdConvertService.ts`
  - `frontend/src/pages/MdConvert/index.tsx`
  - `frontend/src/pages/MdConvert/index.less`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes`：成功
  - `cd frontend && npm run build`：成功（存在既有 webpack 体积告警与 Arco less deprecation 告警，不影响本次功能构建通过）
- 风险：
  - 中：`.doc` 与复杂 PDF 当前优先走降级文本提取，结构化 Markdown 质量不如 `.docx/.xlsx/.html`。
  - 中：扫描件 PDF、图片文件仍未接 OCR，本次不支持。
  - 低：当前结果为即时转换，不保留转换历史。
