# 2026-03-23

## 知识集中知识来源改为卡片展示
- 结论：已将知识集中的知识来源列表从表格切换为卡片展示，卡片展示名称、类型、状态、语言、描述、安全内容摘要、创建人和创建时间；抽屉内默认两列，独立页面默认三列。
- 改动文件：
  - `frontend/src/pages/KnowledgeSource/index.tsx`
  - `frontend/src/pages/KnowledgeSource/style/index.less`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 42s`
  - `cd frontend && timeout 180s npm run build` ⚠️ 命令超时退出（code 124），但 `frontend/dist` 构建产物时间已刷新到 `2026-03-23 10:47`，说明 webpack 已实际产出新包；终端会话仍未稳定回传最终完成态。
- 风险：
  - 低：仅调整知识来源前端展示，不涉及后端接口和数据结构。
  - 低：前端构建存在既有“构建完成但进程不及时退出/终端不回传最终态”的现象，本次未继续追查构建链路。

## 知识集增加系统内置属性并限制内置项为只读
- 结论：已复用后端已有 `is_system` 字段，对外透出为知识集 DTO 的 `isSystem` 属性；初始化生成的知识集继续保留 `is_system = true`。知识集管理页新增“系统内置”列，系统内置知识集仅保留“查看来源”，不再允许检索测试、编辑、删除；来源抽屉也切为只读。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/knowledgeset/dto/KnowledgeSetDto.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/service/impl/KnowledgeSetServiceImpl.java`
  - `frontend/src/pages/KnowledgeSet/index.tsx`
  - `frontend/src/pages/KnowledgeSource/index.tsx`
  - `frontend/src/pages/KnowledgeSource/style/index.less`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 1m 11s`
  - `cd frontend && timeout 360s npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings in 186200 ms`
- 风险：
  - 低：本次不新增数据库字段，只是把已有 `is_system` 透出到前端，并补充后端更新保护。
  - 中：当前“系统内置”的只读限制主要落在知识集管理页和知识来源抽屉；如果后续还有其他入口直接编辑知识集，需要沿用同样的只读判断。

## 移除知识来源 language 属性并收紧卡片展示
- 结论：已从知识来源前后端模型中移除 `language` 属性，新增/编辑表单不再展示语言输入；知识来源列表保持单列卡片，且卡片头部不再展示成功/失败状态标签。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/knowledgeset/entity/KnowledgeSource.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/dto/KnowledgeSourceDto.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/dto/KnowledgeSourceCreateDto.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/dto/KnowledgeSourceUpdateDto.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/service/impl/KnowledgeSourceServiceImpl.java`
  - `frontend/src/pages/KnowledgeSource/index.tsx`
  - `frontend/src/pages/KnowledgeSource/components/AddEditKnowledgeSourceModal.tsx`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 58s`
  - `cd frontend && timeout 360s npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings in 266866 ms`
- 风险：
  - 低：本次未删除数据库中的 `knowledge_source.language` 实际列，只是停止在代码层读写该字段，避免本次任务引入额外迁移。
  - 低：现有历史数据中的语言值会保留在数据库中，但前后端均不再展示和使用。

## 需求开发：系统助手聊天渲染与样式、需求列表操作列收窄
- 结论：已按 `quiz-requirement-develop` 链路推进并完成 4 条 OPEN 需求，覆盖系统助手 Markdown 渲染、横向滚动条、用户消息可读性、需求列表操作列宽度四项问题。
- 改动文件：
  - `frontend/src/components/GlobalAssistant/index.tsx`
  - `frontend/src/components/GlobalAssistant/style.less`
  - `frontend/src/components/ChatReferenceList/index.tsx`
  - `frontend/src/pages/Requirement/index.tsx`
- 验证：
  - `cd backend && gradle classes` ✅ `BUILD SUCCESSFUL in 46s`
  - `cd frontend && npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings in 352507 ms`
- 风险：
  - 低：仅前端展示与样式调整，不涉及后端接口、数据库结构与状态机逻辑。
  - 低：构建 warning 为既有包体积与 less 依赖告警，本次改动未新增高风险 warning。

## 知识来源卡片去掉描述展示
- 结论：知识来源列表继续保持单列卡片展示，并去掉卡片中的“描述”区块，只保留类型/状态、内容概览、创建人和创建时间。
- 改动文件：
  - `frontend/src/pages/KnowledgeSource/index.tsx`
  - `frontend/src/pages/KnowledgeSource/style/index.less`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 18s`
  - `cd frontend && timeout 360s npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings in 355500 ms`
- 风险：
  - 低：仅调整前端展示层，不影响知识来源数据结构和接口契约。

## 知识来源卡片标题限制长度
- 结论：已将知识来源卡片标题改为单行省略，长标题会截断并通过原生 `title` 展示完整内容；标签区同步改为不换行，避免被长标题挤到下一行。
- 改动文件：
  - `frontend/src/pages/KnowledgeSource/index.tsx`
  - `frontend/src/pages/KnowledgeSource/style/index.less`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 12s`
  - `cd frontend && timeout 360s npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings in 197249 ms`
- 风险：
  - 低：仅调整卡片标题和标签布局，不影响接口与数据。

## 内置知识集仅查看标签移出操作列
- 结论：已将系统内置知识集的 `仅查看` 标签从操作列移出，改为与“系统内置”状态放在同一列展示；操作列现在只保留动作按钮，列表观感更干净。
- 改动文件：
  - `frontend/src/pages/KnowledgeSet/index.tsx`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 9s`
  - `cd frontend && timeout 360s npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings in 227079 ms`
- 风险：
  - 低：仅调整知识集列表前端列渲染，不涉及后端接口、数据结构或只读权限逻辑。

## quiz-requirement-develop：串行检查点与中断续跑
- 结论：已为 `quiz-requirement-develop` 增加“严格串行逐条处理 + 每条完成即时检查点输出 + 检查点文件续跑”能力，满足中断后从最近检查点继续而非全量重扫。
- 改动文件：
  - `skills/quiz-requirement-develop/scripts/develop_requirement.py`
  - `skills/quiz-requirement-develop/SKILL.md`
- 验证：
  - `python3 -m py_compile skills/quiz-requirement-develop/scripts/develop_requirement.py` ✅
  - `python3 skills/quiz-requirement-develop/scripts/develop_requirement.py --help` ✅（新增参数可见：`--checkpoint-file --resume --reset-checkpoint --checkpoint-log-file --checkpoint-stream`）
  - `cd backend && gradle classes` ✅ `BUILD SUCCESSFUL in 7s`
  - `cd frontend && npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings in 201733 ms`
- 风险：
  - 中：若进程在“单条需求执行中间步骤”被强杀，该条需求可能已发生部分状态写回；续跑会从上一个已落盘检查点继续（即重试当前条），不会从头扫全量。
  - 低：检查点文件默认落在技能目录 `runtime/`，建议在生产执行时显式指定 `--checkpoint-file` 到持久化路径并做好备份。

## 优化 quiz-requirement-develop：串行执行 + 检查点 + 中断续跑
- 结论：`quiz-requirement-develop` 已升级为严格串行处理（auto-query 固定逐条执行），并新增“每完成 1 条立即输出检查点 + 本地检查点状态持久化 + --resume 续跑”机制，支持进程中断后从最近检查点继续，不再从头全量重扫。
- 改动文件：
  - `skills/quiz-requirement-develop/scripts/develop_requirement.py`
  - `skills/quiz-requirement-develop/SKILL.md`
  - `.gitignore`（忽略 `skills/quiz-requirement-develop/runtime/`）
- 验证：
  - `python3 -m py_compile skills/quiz-requirement-develop/scripts/develop_requirement.py` ✅
  - `cd backend && gradle classes` ✅ `BUILD SUCCESSFUL in 7s`
  - `cd frontend && npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings in 201733 ms`
- 风险：
  - 中：检查点文件基于本地文件系统，跨机器/跨路径执行需显式传同一 `--checkpoint-file` 才能续跑。
  - 低：为防误覆盖，存在检查点文件时默认拒绝“重新全量跑”；需要显式 `--resume` 或 `--reset-checkpoint`。
