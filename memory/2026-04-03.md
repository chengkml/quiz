## 需求管理增加创建时间列
- 结论：已在 Requirement 列表页新增“创建时间”列，直接展示后端已返回的 `createDate`，并同步放大表格横向滚动宽度；需求 `283634228569047421` 已回写到 `COMPLETED`。
- 改动文件：
  - `frontend/src/pages/Requirement/index.tsx`
- 改动说明：
  - 在需求列表 `columns` 中新增 `createDate` 列，复用 `renderDate` 做时间格式化，空值兜底显示 `-`。
  - 将表格 `scroll.x` 从 `1310` 调整到 `1470`，避免新增列后横向空间过窄。
  - 后端链路未改动；`RequirementDto` 继承基础 `Dto`，原本已具备 `createDate` 返回能力。
- 验证：
  - 未执行 `npm run build` / `gradle classes`；按本次任务要求默认跳过编译/构建检查。
  - 已通过需求状态接口完成进度写回：`IN_PROGRESS(0 -> 60 -> 90)`，最终 `COMPLETED(100)`。
- 风险：
  - 低。仅影响 Requirement 列表页前端列展示与横向滚动宽度。
  - 当前工作区存在其他未提交改动（Requirement 页此前筛选/操作列相关改动也在同文件中），本次仅追加“创建时间”列相关最小改动，提交时需注意区分变更范围。
