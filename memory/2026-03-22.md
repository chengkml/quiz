# 2026-03-22

## 定位系统中的 AI 助手功能和知识集管理功能
- 结论：已定位到 AI 助手主链路位于 `Chat` 模块与 `GlobalAssistant` 全局悬浮助手，知识集管理主链路位于 `KnowledgeSet` / `KnowledgeSource` / `Vector` 模块；并补充了模块索引中的 AI 助手条目。
- 改动文件：
  - `memory/ENGINEERING_MODULE_INDEX.md`
  - `memory/2026-03-22.md`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 36s`
  - `cd frontend && npm run build` ⚠️ 打包过程中仅稳定拿到 `baseline-browser-mapping` 提示，执行会话未返回最终退出态；但 `frontend/dist` 构建产物时间已刷新到 `2026-03-22 09:25`，建议本地再复跑一次确认。
- 风险：
  - 低：本次仅做代码定位与文档沉淀，不改业务逻辑。
  - 中：后端 `ChatCompletionRequest` 已支持 `knowledgeSetId` 做 RAG 检索，但前端 `Chat` / `GlobalAssistant` 当前未传该字段，AI 助手与知识集尚未在现有 UI 上直接打通。

## 全局 AI 助手接入知识集 RAG 问答
- 结论：已完成一期改造。页头全局助手默认按“全部可访问知识集”问答，支持切换到单个知识集；后端统一做知识范围解析、启用状态校验、权限过滤和多知识集检索，未命中知识时直接返回明确提示，不再回退为模型自由发挥。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/chat/dto/ChatCompletionRequest.java`
  - `backend/src/main/java/com/ck/quiz/chat/service/impl/ChatServiceImpl.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/dto/VectorSearchFilter.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/repository/KnowledgeSetRepository.java`
  - `backend/src/main/java/com/ck/quiz/knowledgeset/service/impl/VectorServiceImpl.java`
  - `frontend/src/components/GlobalAssistant/index.tsx`
  - `frontend/src/components/GlobalAssistant/style.less`
  - `memory/2026-03-22.md`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 1m 4s`
  - `cd frontend && npm run build` ✅ `webpack 5.101.3 compiled with 2 warnings`
- 风险：
  - 中：当前仅改全局助手，独立 `Chat` 页面仍是通用对话模式，未暴露知识集选择能力。
  - 中：RAG 命中结果当前只拼接切片内容，未返回知识集/知识来源引用信息，回答可解释性有限。
  - 低：全局助手的知识集下拉列表当前通过 `my-created + my-joined` 拉取前 200 条启用知识集，若后续知识集规模继续增大，需要改成专用可访问知识集接口或远程搜索选择。

## AI 助手二期：引用来源展示 + 独立 Chat 页面知识范围复用
- 结论：已完成二期改造。后端为知识问答补充引用来源 DTO，并把引用来源持久化到 `chat_message.meta`；聊天会话把知识范围持久化到 `chat_session.extra_config`。前端全局助手和独立 `Chat` 页面都已支持展示引用来源，独立 `Chat` 页面也已支持“全部知识集 / 指定知识集”范围选择，并在切换范围时重置为新会话。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/chat/dto/ChatCompletionResponse.java`
  - `backend/src/main/java/com/ck/quiz/chat/dto/ChatMessageDto.java`
  - `backend/src/main/java/com/ck/quiz/chat/dto/ChatReferenceDto.java`
  - `backend/src/main/java/com/ck/quiz/chat/dto/ChatSessionDto.java`
  - `backend/src/main/java/com/ck/quiz/chat/dto/ChatSessionExtraConfigDto.java`
  - `backend/src/main/java/com/ck/quiz/chat/entity/ChatMessage.java`
  - `backend/src/main/java/com/ck/quiz/chat/service/impl/ChatServiceImpl.java`
  - `backend/src/main/resources/db/migration/V202603221100__add_meta_to_chat_message.sql`
  - `frontend/src/components/ChatReferenceList/index.tsx`
  - `frontend/src/components/GlobalAssistant/index.tsx`
  - `frontend/src/pages/Chat/index.tsx`
  - `frontend/src/pages/Chat/style.css`
  - `frontend/src/services/knowledgeScopeService.ts`
  - `memory/2026-03-22.md`
- 验证：
  - `cd backend && GRADLE_OPTS='-Dorg.gradle.native=false' gradle classes` ✅ `BUILD SUCCESSFUL in 32s`
  - `cd frontend && npm run build` ⚠️ 终端会话未稳定回传最终退出态，但 `frontend/dist` 构建产物已刷新到 `2026-03-22 10:09`
  - `cd frontend && npx tsc --noEmit` ❌ 当前仓库存在既有类型问题：`node_modules/@types/d3-dispatch` 与 `src/pages/Requirement/index.tsx:930`
- 风险：
  - 中：新增 `chat_message.meta` 字段依赖数据库迁移 `V202603221100__add_meta_to_chat_message.sql` 落库后，历史会话的引用来源才能完整持久化。
  - 中：当前独立 `Chat` 页面默认进入知识问答模式；旧会话如果原本是非知识问答对话，再继续发送消息时会按当前选择的知识范围走 RAG。
  - 低：引用来源当前展示为“知识集 / 知识来源 / 片段序号 + distance”，未提供跳转到知识来源详情页的能力。
