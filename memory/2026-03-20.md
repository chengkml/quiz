# 2026-03-20

## cron/job/options 返回补充 KnowledgeProcessingJob
- 结论：已将 `KnowledgeProcessingJob` 加入 `GET /api/cron/job/options` 的作业类型返回列表。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/cron/service/JobService.java`
- 验证：
  - `cd backend && gradle classes` ❌ 当前环境 Gradle 启动失败：`Failed to load native library 'libnative-platform.so'`
  - `cd frontend && npm run build` ⚠️ 命令启动并输出既有 `baseline-browser-mapping` 提示，但工具会话未正常收口，未拿到最终退出状态
- 风险：
  - 低：仅补充作业选项白名单，不改任务执行逻辑与接口结构。

## 新增 MD 知识来源触发异步任务报 `Timestamp` 强转异常
- 结论：已修复 `AbstractAsyncJob` 读取 `job.start_time` 时把 `java.sql.Timestamp` 直接强转为 `LocalDateTime` 的问题，MD 知识来源触发 `KnowledgeProcessingJob` 时不再在 `fire()` 入口报该异常。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/cron/exec/AbstractAsyncJob.java`
- 验证：
  - `cd backend && gradle classes` ❌ 当前环境 Gradle 启动失败：`Failed to load native library 'libnative-platform.so'`
- 风险：
  - 低：仅补充异步 Job 基类的时间类型兼容逻辑；未调整任务状态流转或知识库处理业务逻辑。

## MD 知识来源向量入库报 `KnowledgeChunk` 主键未赋值
- 结论：已在向量入库服务统一为缺失 ID 的 `KnowledgeChunk` 补齐主键，修复 `saveAll` 时 `IdentifierGenerationException`。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/knowledgeset/service/impl/VectorServiceImpl.java`
- 验证：
  - `cd backend && gradle classes` ❌ 当前环境 Gradle 启动失败：`Failed to load native library 'libnative-platform.so'`
- 风险：
  - 低：仅补充 `KnowledgeChunk` 保存前的主键兜底逻辑；不改 embedding 与向量表写入流程。

## quiz-requirement-develop 移除 `--dry-run`
- 结论：已移除 `quiz-requirement-develop` skill 及脚本里的 `--dry-run` 分支，`query` 模式保留只读查询能力，`trajectory` 仅记录实际状态写回结果。
- 改动文件：
  - `skills/quiz-requirement-develop/SKILL.md`
  - `skills/quiz-requirement-develop/scripts/develop_requirement.py`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `python3 -m py_compile skills/quiz-requirement-develop/scripts/develop_requirement.py` ✅
  - `cd backend && gradle classes` ❌ 当前环境 Gradle 启动失败：`Failed to load native library 'libnative-platform.so'`
  - `cd frontend && timeout 180s npm run build` ⚠️ 输出 `baseline-browser-mapping` 历史提示后工具会话未返回最终退出码，未拿到成功态
- 风险：
  - 中：脚本参数面向自动化调用方，若外部仍传 `--dry-run` 会在参数校验阶段直接失败；业务接口与需求状态机未改动。
