# 2026-03-27

## MCP 工具加载改造：兼容阿里云 WebParser SSE MCP
- 结论：
  - 已按“最小改动方案”完成 MCP SSE 兼容改造，保留现有 Controller / Service / DB 结构，未改库表。
  - 后端已兼容 `event:endpoint` 握手返回的 message endpoint，并保留 `session_id` 回退逻辑。
  - `initialize` 后已补发 `notifications/initialized`；`handshake / initialize / tools/list / tools/call` 已补 429 / 瞬时失败退避重试。
  - 健康检查已改为 SSE 兼容方式：握手 + initialize 成功即判定 `ACTIVE`。
  - 前端已去掉抽屉内“加载工具”触发的重复 discovery，避免短时间二次探测。
- 改动文件：
  - `backend/src/main/java/com/ck/quiz/mcp/service/impl/McpServerServiceImpl.java`
  - `frontend/src/pages/McpServer/index.tsx`
- 验证：
  - `cd backend && gradle classes` ✅
  - `cd frontend && npm run build` ✅（exit code 0；存在仓库既有 webpack 体积 warning / less deprecation warning，不影响本次交付）
- 风险：
  - 中：当前 SSE 事件解析优先兼容 `event:endpoint`、JSON `endpoint`、`session_id` 三类返回；若目标 MCP 服务使用更特殊的私有事件格式，仍需按实际报文再补一层兼容。
  - 低：`notifications/initialized` 发送失败时当前按 warn 记录并继续，目的是兼容部分旧服务；若某些新服务强依赖该通知且对失败敏感，需要结合真实服务日志再收紧策略。

## 密钥管理功能改造（需求 273737129270446530）
- 结论：
  - 已完成密码模块本次需求代码落地：去掉分组能力、移除用户名复制、修复验证码接口链路、升级密码存储方案并兼容历史密文。
  - 存储方案由固定默认密钥 + AES/ECB 改为 `PBKDF2WithHmacSHA256 + 随机 salt + 随机 IV + AES/GCM`；历史旧密文在成功查看明文时会自动迁移为新格式。
- 改动文件：
  - `frontend/src/pages/PasswordManager/index.tsx`
  - `frontend/src/pages/PasswordManager/api.ts`
  - `backend/src/main/java/com/ck/quiz/password/controller/PasswordController.java`
  - `backend/src/main/java/com/ck/quiz/password/service/PasswordService.java`
  - `backend/src/main/java/com/ck/quiz/password/entity/PasswordEntry.java`
  - `backend/src/main/java/com/ck/quiz/password/dto/PasswordCreateDto.java`
  - `backend/src/main/java/com/ck/quiz/password/dto/PasswordUpdateDto.java`
  - `backend/src/main/java/com/ck/quiz/password/dto/PasswordDto.java`
  - `backend/src/main/java/com/ck/quiz/password/dto/PasswordQueryDto.java`
  - `backend/src/main/java/com/ck/quiz/utils/EncryptUtil.java`
  - `memory/ENGINEERING_MODULE_INDEX.md`
- 验证：
  - `cd backend && gradle classes` ✅
  - `cd frontend && npm run build`：执行中，待最终结果确认
- 风险：
  - 中：`password_data` 长度由 512 提升到 1024，依赖生产环境 `ddl-auto=update` 自动扩列；若目标环境禁用自动 DDL，需要补 SQL 变更。
  - 低：历史密文采用“查看时自动迁移”策略，未做一次性全量重加密；未被查看/编辑的老数据仍保持旧格式，直到后续触达。
